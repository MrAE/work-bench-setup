# README

Home-Dir and Workbenches setup scripts.

## Instructions

- run `setup-alpha.sh`, expects a directory called ALPHA in your home directory to then create a workbench hierarchy.
- move `new-workbench.sh` to the directory created. 


